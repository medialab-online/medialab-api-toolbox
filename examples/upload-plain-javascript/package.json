{
  "name": "medialab-direct-upload",
  "version": "0.0.1",
  "license": "MIT",
  "devDependencies": {
    "browser-sync": "^2.29.3"
  },
  "scripts": {
    "start": "yarn check-config && browser-sync",
    "check-config": "if [ ! -f javascript/config.js ]; then echo 'config.js not found'; exit 1; fi; exit 0"
  }
}
